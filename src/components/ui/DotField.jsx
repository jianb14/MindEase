export default function DotField() {
  return (
    <div
      className="fixed inset-0 pointer-events-none -z-10 bg-bg-primary"
      style={{
        backgroundImage:
          "radial-gradient(rgba(19,143,231,0.2) 1px, transparent 1px)",
        backgroundSize: "18px 18px",
      }}
    />
  );
}